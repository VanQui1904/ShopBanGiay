@model ShoeStoreProject.Areas.Admin.Data.BrandViewModel

@{
    ViewBag.Title = "Delete Brand";
    Layout = "~/Areas/Admin/Views/Shared/LayoutAdmin.cshtml";
}

<div class="container mt-5">
    <div class="card">
        <div class="card-header">
            <h2 class="text-danger">Delete Brand</h2>
        </div>
        <div class="card-body">
            <h3>Are you sure you want to delete this brand?</h3>
            <hr />
            <dl class="row">
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.BrandId)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.BrandId)
                </dd>

                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.BrandName)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.BrandName)
                </dd>
            </dl>
        </div>
        <div class="card-footer">
            @using (Html.BeginForm("DeleteBrand", "Brand", FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(model => model.BrandId)
                <div class="form-actions">
                    <input type="submit" value="Delete" class="btn btn-danger" />
                    @Html.ActionLink("Cancel", "Index", "Brand", null, new { @class = "btn btn-secondary ml-2" })
                </div>
            }
        </div>
    </div>
</div>
